# The relative path to the local HTML file starting from the project root
# This is combined with System.getProperty("user.dir") in the BaseTest
testHtmlPath=/src/test/resources/login.html

# The target browser for execution (chrome, firefox, edge)
browser=chrome